# 小信号微信小程序

原生微信小程序工程，使用 TypeScript、WXML 与 WXSS。当前为 Task 01 初始化阶段，页面均为占位结构。

## 本地检查

```sh
npm install
npm run typecheck
```

使用微信开发者工具打开本目录；小程序根目录为 `miniprogram/`。`project.config.json` 中的 `appid` 是占位值，需在实际开发者账号配置后替换。

数据层目录预留在 `miniprogram/repositories/mock/`；当前没有登录、业务数据、数据库或云函数实现。
